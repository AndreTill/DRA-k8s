# Prod Inference: MIG 3g.40gb через ResourceClaimTemplate
apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-inference-mig
  namespace: inference-prod
  labels:
    workload-type: inference
    gpu-mode: mig
spec:
  replicas: 2
  selector:
    matchLabels:
      app: llm-inference-mig
  template:
    metadata:
      labels:
        app: llm-inference-mig
        gpu-mode: mig
    spec:
      resourceClaims:
        - name: gpu
          resourceClaimTemplateName: rct-mig-3g40
      containers:
      - name: vllm
        image: vllm/vllm-openai:latest
        args:
          - "--model"
          - "meta-llama/Llama-3.1-8B-Instruct"
          - "--max-model-len"
          - "4096"
          - "--gpu-memory-utilization"
          - "0.90"
        ports:
        - name: http
          containerPort: 8000
        resources:
          requests:
            cpu: "4"
            memory: "16Gi"
          limits:
            cpu: "8"
            memory: "32Gi"
          claims:
            - name: gpu
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 6
      tolerations:
      - key: nvidia.com/gpu
        operator: Exists
        effect: NoSchedule
---
apiVersion: v1
kind: Service
metadata:
  name: llm-inference-mig
  namespace: inference-prod
spec:
  selector:
    app: llm-inference-mig
  ports:
  - name: http
    port: 8000
    targetPort: 8000
